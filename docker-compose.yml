version: '3.8'

services:

  db:
    image: mysql:8.0
    container_name: db_cont
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: notesdb
      MYSQL_USER: notesuser
      MYSQL_PASSWORD: notespass
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  django:
    build: .
    container_name: django_cont
    command: sh -c "sleep 15 && python manage.py migrate && gunicorn notesapp.wsgi:application --bind 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      DB_NAME: notesdb
      DB_USER: notesuser
      DB_PASSWORD: notespass
      DB_HOST: db
      DB_PORT: 3306

  nginx:
    image: nginx:latest
    container_name: nginx_cont
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - django

volumes:
  mysql_data:
